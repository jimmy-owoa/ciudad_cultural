<%= form_with(model: user_member, local: true) do |form| %>
  <% if user_member.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_member.errors.count, "error") %> prohibited this user_member from being saved:</h2>

      <ul>
      <% user_member.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
 <div class="card">
    <div class="card-header">
      <h4>Formulario</h4>
    </div>
    <div class="card-body">
      <div class="form-group">
        <%= form.label :name %>
        <%= form.text_field :name, class: 'form-control' %>
      </div>
      <div class="form-group">
        <%= form.label :lastname %>
        <%= form.text_field :lastname, class: 'form-control' %>
      </div>
      <div class="form-group">
        <%= form.label :email %>
        <%= form.email_field :email, class: 'form-control' %>
      </div>
      <div class="form-group">
        <%= form.label :summary %>
        <%= form.text_area :summary, class: 'form-control' %>
      </div>
      <div class="form-group">
        <%= form.label :phone %>
        <%= form.text_field :phone, class: 'form-control' %>
      </div>
      <div class="card-footer text-right">
        <%= form.submit class:"btn btn-primary mr-1" %>
      </div>
    </div>
  </div>
<% end %>
