<p id="notice"><%= notice %></p>
<div class="section-header">
  <div class="text-uppercase font-weight-bold-max display-4"><%= @user_event.title %></div>
</div>
<div class="card">
  <div class="card-body">
    <p>
      <%= @user_event.description %>
    </p>

    <p>
      <strong>Date:</strong>
      <%= @user_event.date.strftime("%d-%m-%y") %>
    </p>
    <p>
      <strong>Galería</strong>
    </p>
      <% @user_event.images.each do |image| %>
        <%= image_tag image.variant(resize: '500x'), style: 'height: 200px; width: 300px; object-fit: cover;' %>
      <% end %>
  </div>
</div>

<%= link_to 'Back', root_path %>
