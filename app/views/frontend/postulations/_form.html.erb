<%= form_with(model: postulation, local: true) do |form| %>
  <% if postulation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(postulation.errors.count, "error") %> prohibited this postulation from being saved:</h2>

      <ul>
      <% postulation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :lastname %>
    <%= form.text_field :lastname %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email %>
  </div>

  <div class="field">
    <%= form.label :message %>
    <%= form.text_area :message %>
  </div>

  <div class="field">
    <%= form.label :user_event_id %>
    <%= form.text_field :user_event_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
