<%= form_with(model: postulation, local: true) do |form| %>
  <% if postulation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(postulation.errors.count, "error") %> prohibited this postulation from being saved:</h2>

      <ul>
      <% postulation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<div class="card p-4">
  <div class="field">
    <%= form.label "Nombre" %>
    <%= form.text_field :name, class: "form-control", required: true %>
  </div>

  <div class="field">
    <%= form.label "Apellidos" %>
    <%= form.text_field :lastname, class: "form-control", required: true %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email, class: "form-control", type: 'email', required: true %>
  </div>

  <div class="field">
    <%= form.label "Mensaje" %>
    <%= form.text_area :message, class: "form-control", required: true %>
  </div>

  <div class="field">
    <%= form.label "Evento" %>
    <%= form.select(:user_event_id, options_for_select(@events.map{|event| [event.title, event.id]}), {}, {class:' form-control'}) %>
  </div>

  <div class="form-group">
    <%= form.label "Fotos" %>
    <%= form.file_field :images, multiple: true, class: 'form-control' %>
  </div>

  <div class="actions">
    <%= form.submit "Enviar Postulación" %>
  </div>
<% end %>

</div>
